package 安卓x.约束布局.组件;

import android.content.Context;
import android.util.AttributeSet;

import androidx.constraintlayout.widget.ConstraintLayout;

/**
 * 创建时间：2025年11月24日.
 * <p>
 * 描述：约束布局
 * @author dxyc
 */
public class 约束布局 extends ConstraintLayout {

    public 约束布局(Context 上下文) {
        super(上下文);
    }

    public 约束布局(Context 上下文, AttributeSet 属性) {
        super(上下文, 属性);
    }

    public 约束布局(Context 上下文, AttributeSet 属性, int 默认样式属性) {
        super(上下文, 属性, 默认样式属性);
    }

    public 约束布局(Context 上下文, AttributeSet 属性, int 默认样式属性, int 默认样式资源) {
        super(上下文, 属性, 默认样式属性, 默认样式资源);
    }

}
