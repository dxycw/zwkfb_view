package 安卓.组件;

import android.content.Context;
import android.util.AttributeSet;
import android.widget.AnalogClock;

/**
 * 创建时间：2025年11月22日.
 * <p>
 * 描述：模拟时钟
 * @author dxyc
 */
public class 模拟时钟 extends AnalogClock {

    public 模拟时钟(Context 上下文) {
        super(上下文);
    }

    public 模拟时钟(Context 上下文, AttributeSet 属性) {
        super(上下文, 属性);
    }

    public 模拟时钟(Context 上下文, AttributeSet 属性, int 默认样式属性) {
        super(上下文, 属性, 默认样式属性);
    }

    public 模拟时钟(Context 上下文, AttributeSet 属性, int 默认样式属性, int 默认样式资源) {
        super(上下文, 属性, 默认样式属性, 默认样式资源);
    }

}
