package 安卓.组件;

import android.content.Context;
import android.util.AttributeSet;
import android.widget.LinearLayout;

/**
 * 创建时间：2025年11月18日.
 * <p>
 * 描述：线性布局
 * @author dxyc
 */
public class 线性布局 extends LinearLayout {

    public 线性布局(Context 上下文) {
        super(上下文);
    }

    public 线性布局(Context 上下文, AttributeSet 属性) {
        super(上下文, 属性);
    }

    public 线性布局(Context 上下文, AttributeSet 属性, int 默认样式属性) {
        super(上下文, 属性, 默认样式属性);
    }

    public 线性布局(Context 上下文, AttributeSet 属性, int 默认样式属性, int 默认样式资源) {
        super(上下文, 属性, 默认样式属性, 默认样式资源);
    }

}
