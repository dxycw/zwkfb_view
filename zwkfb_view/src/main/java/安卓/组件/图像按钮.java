package 安卓.组件;

import android.annotation.SuppressLint;
import android.content.Context;
import android.util.AttributeSet;
import android.widget.ImageButton;

/**
 * 创建时间：2025年11月19日.
 * <p>
 * 描述：图像按钮
 * @author dxyc
 */
@SuppressLint("AppCompatCustomView")
public class 图像按钮 extends ImageButton {

    public 图像按钮(Context 上下文) {
        super(上下文);
    }

    public 图像按钮(Context 上下文, AttributeSet 属性) {
        super(上下文, 属性);
    }

    public 图像按钮(Context 上下文, AttributeSet 属性, int 默认样式属性) {
        super(上下文, 属性, 默认样式属性);
    }

    public 图像按钮(Context 上下文, AttributeSet 属性, int 默认样式属性, int 默认样式资源) {
        super(上下文, 属性, 默认样式属性, 默认样式资源);
    }


}
