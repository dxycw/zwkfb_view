package 安卓.输入法服务;

import android.content.Context;
import android.inputmethodservice.ExtractEditText;
import android.util.AttributeSet;

/**
 * 创建时间：2025年11月24日.
 * <p>
 * 描述：提取编辑文本
 * @author dxyc
 */
public class 提取编辑文本 extends ExtractEditText {

    public 提取编辑文本(Context 上下文) {
        super(上下文);
    }

    public 提取编辑文本(Context 上下文, AttributeSet 属性) {
        super(上下文, 属性);
    }

    public 提取编辑文本(Context 上下文, AttributeSet 属性, int 默认样式属性) {
        super(上下文, 属性, 默认样式属性);
    }

    public 提取编辑文本(Context 上下文, AttributeSet 属性, int 默认样式属性, int 默认样式资源) {
        super(上下文, 属性, 默认样式属性, 默认样式资源);
    }

}
