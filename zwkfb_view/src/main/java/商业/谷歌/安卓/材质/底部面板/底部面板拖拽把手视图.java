package 商业.谷歌.安卓.材质.底部面板;

import android.content.Context;
import android.util.AttributeSet;

import androidx.annotation.NonNull;
import androidx.annotation.Nullable;

import com.google.android.material.bottomsheet.BottomSheetDragHandleView;

/**
 * 创建时间：2025年11月24日.
 * <p>
 * 描述：底部动作条拖拽把手视图
 * @author dxyc
 */
public class 底部面板拖拽把手视图 extends BottomSheetDragHandleView {

    public 底部面板拖拽把手视图(@NonNull Context 上下文) {
        super(上下文);
    }

    public 底部面板拖拽把手视图(@NonNull Context 上下文, @Nullable AttributeSet 属性) {
        super(上下文, 属性);
    }

    public 底部面板拖拽把手视图(@NonNull Context 上下文, @Nullable AttributeSet 属性, int 默认样式属性) {
        super(上下文, 属性, 默认样式属性);
    }


}
