package 商业.谷歌.安卓.材质.内部;

import android.annotation.SuppressLint;
import android.content.Context;
import android.util.AttributeSet;

import com.google.android.material.internal.BaselineLayout;

/**
 * 创建时间：2025年11月24日.
 * <p>
 * 描述：基线布局
 * @author dxyc
 */
public class 基线布局 extends BaselineLayout {

    @SuppressLint("RestrictedApi")
    public 基线布局(Context 上下文) {
        super(上下文);
    }

    @SuppressLint("RestrictedApi")
    public 基线布局(Context 上下文, AttributeSet 属性) {
        super(上下文, 属性);
    }

    @SuppressLint("RestrictedApi")
    public 基线布局(Context 上下文, AttributeSet 属性, int 默认样式属性) {
        super(上下文, 属性, 默认样式属性);
    }

}
